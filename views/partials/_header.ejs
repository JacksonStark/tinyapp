<header>
    <nav class="navbar navbar-expand-md navbar-dark bg-success">
      <a class="navbar-brand" href="/urls">TinyApp</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
          <a class="nav-item nav-link" href="/urls">My URLs</a>
          <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
        </div>
      </div>
      <% if(username) { %>
        Logged In As: <%= username %>
          <form method='POST' action='/logout' style='float: right'>
          <button type='submit' class='btn btn-primary'>Logout</button>
        </form>
      <% } else { %>
      <form class="form-inline" method='POST' action='/login'>
          <input type='text' name='username' class='form-control mr-sm-2' placeholder='username' style='width:200px'>
          <button type='submit' class='btn btn-primary my-2 my-sm-0'>Sign In</button>
        </form>
      <% } %>
    </nav>
  </header>
